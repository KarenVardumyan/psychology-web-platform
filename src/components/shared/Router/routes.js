
import { Home, Tests } from 'components/pages';

const routeObjects = [
  {
    index: true,
    element: <Home />,
  },
  {
    path: 'tests',
    element: <Tests />
  }
]

export default routeObjects;